<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".VeloLauncherApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="VeloLauncher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VeloLauncher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Ride summary screen — launchable by external apps after a workout -->
        <activity
            android:name=".RideSummaryActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.VeloLauncher">
            <intent-filter>
                <action android:name="io.freewheel.launcher.SHOW_RIDE_SUMMARY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Ride logging receiver for third-party fitness apps -->
        <receiver
            android:name=".RideLogReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.freewheel.launcher.ACTION_LOG_RIDE" />
            </intent-filter>
        </receiver>

        <!-- ContentProvider: exposes workouts, rides, profile, session to other apps -->
        <provider
            android:name=".data.VeloContentProvider"
            android:authorities="io.freewheel.launcher.provider"
            android:exported="true"
            android:grantUriPermissions="true" />

        <!-- WorkoutSession AIDL service: external apps bind to control workouts -->
        <service
            android:name=".session.WorkoutSessionService"
            android:exported="true">
            <intent-filter>
                <action android:name="io.freewheel.launcher.WORKOUT_SESSION" />
            </intent-filter>
        </service>

        <!-- FileProvider for sharing APK with package installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Floating stats overlay service -->
        <service
            android:name=".overlay.RideOverlayService"
            android:exported="false" />

        <!-- Floating home button overlay (shown when apps are in foreground) -->
        <service
            android:name=".overlay.HomeButtonOverlay"
            android:exported="false" />
    </application>
</manifest>
